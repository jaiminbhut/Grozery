const loginType = {
  facebook: 'facebook',
  google: 'google',
  mobile: 'mobile',
};

const locationSelection = {
  state: 'state',
  city: 'city',
  area: 'area',
};

const Keys = { loginType, locationSelection };

export default Keys;
